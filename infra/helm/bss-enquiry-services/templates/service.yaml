apiVersion: v1
kind: Service
metadata:
  name: bss-core-dmdm-svc
spec:
  selector: { app: bss-core-dmdm-services }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
